/* Check if a linked list is a palindrome
*/

//just put in an array and then do the palindrome algorithm like normal

class Node {
  constructor (data) {
    this.data = data;
    this.next = null;
  }
}